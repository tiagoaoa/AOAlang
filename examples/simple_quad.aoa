decl private x
decl deferred a, b

# Compute x^2
x_squared = x * x

# Compute a*x
ax = a * x

# Compute x^2 + a*x + b
sum1 = x_squared + ax
poly_result = sum1 + b

# Enforce poly_result == 0
zero_const = 0
check = poly_result == zero_const
